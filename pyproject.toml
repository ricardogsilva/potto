[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "potto"
version = "0.1.0"
description = "A starlette application that wraps pygeoapi"
readme = "README.md"
authors = [
    { name = "Ricardo Garcia Silva", email = "ricardo.garcia.silva@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "alembic>=1.18.0",
    "alembic-postgresql-enum>=1.8.0",
    "babel>=2.17.0",
    "bcrypt>=5.0.0",
    "cryptography>=50.0.0",
    "cyclopts>=4.2.4",
    "duckdb>=1.5.3",
    "fastapi[standard]>=0.128.0",
    "geoalchemy2>=0.18.1",
    "geopandas>=1.1.3",
    "httpx>=0.28.1",
    "jinja2>=3.1.6",
    "ogcapi-registry>=0.3.0",
    "psycopg[binary]>=3.3.3",
    "pydantic>=2.12.4",
    "pydantic-settings>=2.14.2",
    "pygeoapi>=0.22.0",
    "pygeofilter[backend-native,backend-sqlalchemy]>=0.3.3",
    "pygments>=2.19.2",
    "pyjwt>=2.13.0",
    "pyogrio>=0.12.1",
    "pyproj>=3.7.2",
    "pyyaml>=6.0.3",
    "rich>=14.2.0",
    "shapely>=2.1.2",
    "sqlalchemy>=2.0.45",
    "sqlmodel>=0.0.31",
    "starlette>=1.3.1",
    "starlette-admin>=0.16.0",
    "starlette-babel>=1.0.3",
    "uvicorn>=0.38.0",
]

[dependency-groups]
dev = [
    "ogc-cite-runner>=0.3.2",
    "playwright>=1.62.0",
    "pytest>=9.0.3",
    "pytest-asyncio>=1.3.0",
    "pytest-playwright>=0.9.0",
    "ruff>=0.15.12",
    "ty>=0.0.33",
]
docs = [
    "zensical>=0.0.38",
]

[project.scripts]
potto = "potto.__main__:potto_app.meta"

[tool.uv]
default-groups = []

[tool.pytest.ini_options]
addopts = "--strict-markers --verbose"
markers = [
    "integration: marks tests as being integration tests",
    "e2e: marks tests as being end-to-end tests that need a live server and a real browser",
]

[tool.ty.src]
exclude = [
    "src/potto/managers/postgis/db/migrations",
    "tests"
]
